'use strict';

/**
 * @ngdoc function
 * @name engramatronApp.controller:IdeaCtrl
 * @description
 * # IdeaCtrl
 * Controller of the engramatronApp
 */
angular.module('engramatronApp')
  .controller('IdeaCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
